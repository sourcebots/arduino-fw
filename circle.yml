dependencies:
    pre:
        - sudo apt-get install arduino arduino-core arduino-mk
        - git clone https://github.com/scottdarch/Arturo && cd Arturo && sudo make install

test:
    override:
        - ano build -m uno
